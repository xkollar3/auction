axon:
  eventhandling:
    processors:
      user_read_model:
        mode: subscribing

server:
  error:
    include-stacktrace: never

policy:
  refund-deadline-days: 30
  commission-percentage: 0.1

spring:
  jpa:
    open-in-view: false
